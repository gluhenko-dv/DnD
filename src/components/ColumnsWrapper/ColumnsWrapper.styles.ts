import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
  columns: {
    display: 'flex',
    overflow: 'hidden',
    overflowX: 'scroll',
    height: '95vh',
    padding: '10px 5px 20px',
    width: '100%'
  }
}));
