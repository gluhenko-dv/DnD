import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
  item: {
    width: '100%',
    padding: '10px',
    backgroundColor: 'rgb(204, 204, 204)',
    marginBottom: '5px',
    transition: '0.5s'
  },
  title: {
    cursor: 'text'
  }
}));
